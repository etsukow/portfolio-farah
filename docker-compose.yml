# Pull & run the published image on your server:
#   docker compose pull && docker compose up -d
#
# The image is built and pushed to GHCR by GitHub Actions on every push to main.
services:
  farah:
    image: ghcr.io/etsukow/farah:latest
    container_name: farah
    restart: unless-stopped
    ports:
      - "8081:80"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost/"]
      interval: 30s
      timeout: 3s
      retries: 3

    # --- Traefik (same pattern as portfolio) ---
    # labels:
    #   - "traefik.enable=true"
    #   - "traefik.http.routers.farah.rule=Host(`farah.etsukow.com`)"
    #   - "traefik.http.routers.farah.entrypoints=websecure"
    #   - "traefik.http.routers.farah.tls.certresolver=letsencrypt"
    #   - "traefik.http.services.farah.loadbalancer.server.port=80"
